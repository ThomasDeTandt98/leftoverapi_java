spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: users

  flyway:
    enabled: true
    locations: classpath:db/migration/users
    default-schema: users